
#include "BaseTextProcessor.h"

std::vector<std::string> BaseTextProcessor::process(std::vector<std::string> tokens) const {
    // Базовая обработка: ничего не делает, просто возвращает как есть.
    return tokens;
}